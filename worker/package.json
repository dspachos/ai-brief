{
  "name": "ai-brief-mcp-worker",
  "description": "The AI Brief — remote MCP server on Cloudflare Workers (stateless, reads the live site)",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "*",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "wrangler": "^4.115.0"
  }
}
